---
import projectsData from "../constants/projects.json";
import ProjectCard from "../components/ProjectCard.astro";
import Layout from "../layouts/Layout.astro";

const projects = projectsData;
---

<Layout title="1hemmem • Projects">
  <section class="pt-8">
    <h1 class="text-primary font-serif text-[2.5rem] tracking-tight">
      Projects
    </h1>
    <h2 class="text-title text-base tracking-tight">
      Projects are my go-to for learning new technologies and applying them.
      Each project represents a journey of exploration and skill-building.
    </h2>
    <br />
    <div class="pt-1.5">
      {projects.map((project) => <ProjectCard project={project} />)}
    </div>
  </section>
</Layout>
